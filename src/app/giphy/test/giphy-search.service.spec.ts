describe('giphy-search.service.spec.ts', () => {
  beforeEach(() => {
  });

  fit('2 deveria ser igual a 2', () => {
    // Entrada
    let a = 0;
    // Processamento
    a = 0 + 1;
    // Saída
    expect(a).toBe(2);
  });

});
